# Tested Python 3.11 runtime. Build Basilisk separately from the recorded source
# commit; its local ARM binary cannot be copied onto a Linux cluster.
# Ray's optional rllib extra requires Gymnasium 0.28.1, whereas the measured
# completion-v2 preflight uses 0.29.1. Install Ray itself and explicitly pin the
# RLlib dependencies below to the tested environment; do not resolve that extra.
ray==2.35.0
torch==2.4.1
numpy==1.26.4
gymnasium==0.29.1
pettingzoo==1.24.3
pyarrow==19.0.1
scipy==1.14.1
psutil==6.1.0
matplotlib==3.9.3
pandas==2.2.3
protobuf==6.30.1
tensorboardX==2.6.2.2
requests==2.32.3
fsspec==2025.3.0
dm-tree==0.1.9
lz4==4.4.3
scikit-image==0.25.1
pyyaml==6.0.2
typer==0.15.2
rich==13.9.4
grpcio==1.71.0
